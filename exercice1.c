#include <stdio.h>


// Remainder: to print an integer, you can use
// print("%d\n", i);
int main(){
    // Declare two ints
    ...

    // Add them and store them in a new variable
    ...

    // Compute the euclidean division of the two integers

    // Store the 3rd bit of the first integer in a new variable

    return 0;

}